<view class="section-header"><view><view class="section-title">{{title}}</view><view wx:if="{{subtitle}}" class="section-subtitle">{{subtitle}}</view></view><view wx:if="{{actionText}}" class="section-action" bindtap="onAction">{{actionText}}</view></view>
