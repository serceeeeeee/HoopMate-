<view class="empty-state"><view class="empty-icon"></view><view class="empty-title">{{title}}</view><view wx:if="{{desc}}" class="empty-desc">{{desc}}</view><button wx:if="{{buttonText}}" class="empty-btn" bindtap="onTap">{{buttonText}}</button></view>
